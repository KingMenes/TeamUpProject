import React from 'react'
import './footer.css'

export default function () {
    return (
        <footer>
            <div>
                React / Express / JavaScript / PostgreSQL / Sequelize
            </div>
        </footer>
    )
}
